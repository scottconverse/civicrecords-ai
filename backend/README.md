# CivicRecords AI Backend Package

This directory is the Python package root for the CivicRecords AI backend.
The project-level README, user manual, changelog, and release notes remain the
operator-facing documentation sources. This package README exists so the wheel
and source distribution are self-describing when published as release assets.
